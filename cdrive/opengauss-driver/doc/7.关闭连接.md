## 关闭连接

在使用数据库连接完成相应的数据操作后，需要关闭数据库连接。

关闭数据库连接有两种方式，一种是直接调用连接的close函数，一种是通过Try-with-resources表达式的方式自动关闭连接。

1. 通过close函数的方式。

   ```
   var conn = ds.connect()
   // 连接SQL处理
   conn.close()
   ```

2. Try-with-resources表达式的方式。

   ```
   try (conn = ds.connect()) {
     // 连接SQL处理
   }
   ```

   